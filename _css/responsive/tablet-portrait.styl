body
	font-size 0.8em

.dots
	font-size 1em
.slider-control
	display block
.card
	position relative
	height cardHeight
	overflow hidden
	if cardRadius
		border-radius cardRadius
	background-color #ccc
	box-sizing border-box
	margin cardMargin
	float left
.card-1
	width cardWidth
.card-2
	width (cardWidth * 2)
.card-3
	width (cardWidth * 3)
.card-4
	width (cardWidth * 3)
#Wrapper
	width (cardWidth * 3)